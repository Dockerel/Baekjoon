#include <bits/stdc++.h>
#define fastIO ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
#define MAX 1000000007

using namespace std;

int t, p, q, r;

void init() { cin >> t; }

void go() {
  cin >> p >> q >> r;
  cout << p*
}

int main() {
  fastIO;
  init();
  for (int i = 0; i < t; i++) {
    go();
  }
  return 0;
}